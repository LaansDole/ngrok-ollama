# ngrok Configuration for Ollama
# Copy this file to .env and update with your settings

# Your ngrok domain. You can reserve one for free at https://dashboard.ngrok.com/domains
# Example: my-ollama-server.ngrok.app
NGROK_DOMAIN=YOUR_RESERVED_DOMAIN.ngrok.app

# Set to 0.0.0.0 to allow remote connections to Ollama
OLLAMA_HOST=0.0.0.0

# Optional: ngrok region (us, eu, ap, au, sa, jp, in)
# Choose the region closest to your users for better performance
# Reduces latency by 20-40ms compared to default routing
# NGROK_REGION=us

# Optional: Log level for debugging (info, debug, warn, error)
# NGROK_LOG_LEVEL=info

# Performance Note: 
# ngrok typically adds 50-100ms latency to API calls
# Run 'make test-latency' to measure actual performance in your environment
# See docs/ngrok-latency-guide.md for optimization tips and alternatives
